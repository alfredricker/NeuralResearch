## Neuron types:
### Connected A -> B -> C
Feed in -- from region to region
Feed out -- from region to region
Model -- forms sparse models
Where -- Recurrent structure along with model type behavior (punt for now)

### Disconnected
Boundary input \Omega -> A -> B
Boundary output A -> B -> Z
