Why does having a u8::MAX number of slots increase memory usage when most of them are "dead" slots? Is it because it necessarily increases the length of the synapse Vec?

Why did you create a dendrite_to_neuron vec on the Dendrite struct when we have a synapse_to_dendrite fn in the same file? Storing them increases memory usage but lowers computational cost. So I suppose this is good, but we shouldn't have both.

Need an efficient and readable way to build arbitrary topologies.
```rust
struct SubGraph {
    name: &'static str
    populations: Vec<Population>
    connections: Vec<Connections>
    input: Option<Vec<u32>> // indices of the input port populations in the population vec
    output: Option<Vec<u32>> // indices of the output port populations in the population vec
}

// connect subgraphs to form a new subgraph
pub fn merge_subgraphs
```