pub enum LearningFuncs {
    Hebbian(f64),
    AntiHebbian(f64),
    STDP(f64),
    Covariance(f64),
    Oja(f64),
    Delta(f64),
    Perceptron(f64),
    Adagrad(f64),
    Adam(f64),
    RMSprop(f64),
    SGD(f64),
    Momentum(f64),
    Nesterov(f64),
    AdaDelta(f64),
    AdaGrad(f64),
    Adamax(f64),
    Nadam(f64),
}