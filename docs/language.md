Write a programming syntax for the network. Goal is to write clean minimal code that compiles into a functional ANN.
Objects should be defined in terms of the following:
- An identifier
- The type of data they expect to receive
- A well defined (optional) transformation on that data. If none is provided, do nothing (identity transform)
- Where the data goes next (can be multichanneled)

